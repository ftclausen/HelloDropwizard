apply plugin: 'java'
apply plugin: 'maven'

repositories {
  mavenCentral()
}

project.ext {
  versions = [:]
}

versions.dropwizard = '1.3.7'

dependencies {
  compile "io.dropwizard:dropwizard-core:$versions.dropwizard"
}
